FROM python:3.11-slim
WORKDIR /app
COPY backend/requirements.txt ./backend/
RUN pip install --no-cache-dir -r backend/requirements.txt
WORKDIR /app/backend
CMD ["uvicorn", "open_webui.main:app", "--host", "0.0.0.0", "--port", "8080", "--reload"]
